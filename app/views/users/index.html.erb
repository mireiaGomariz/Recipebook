<div class="container">
  <div class="row mt-5">
    <div class="col-12 mt-5">
      <%= @user.name %>

      <%= @user.id %>
      <%= image_tag @user.avatar.url, class: "thumbnail" %>
    </div>

  </div>

</div>
